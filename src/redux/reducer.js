// import { createReducer } from "@reduxjs/toolkit";
// import { addContact, changeFilter, deleteContact } from "./actions";
// import { contactsInitialState, filterInitialState } from "./initialState";

// export const contactReducer = createReducer(contactsInitialState, {
//     [addContact]: (state, action) => {state.push(action.payload) },
//     [deleteContact]: (state, action) =>
//     { return state.filter(contact => contact.id !== action.payload) }
// })

// export const filterReducer = createReducer(filterInitialState, {
//      [changeFilter]:(state,action)=>{state = action.payload}
//  }) 

